
var Opening = function () {
	var that = {};

	var adam;
	var eve;
	var attendant;
	var linePerson;

	var start = function() 
	{
		adam = characters[0];
		eve = characters[1];
		attendant = minorCharacters[0];
		linePerson = minorCharacters[1];

		// pan camera

		// adam, eve discuss

		// line moves

		// nudge to move on

		// get pass

		// move to hall entry

		// switch to move on
		
	}

	that.start = start;

	return that; 
}();